<%= link_to t("rules.form.back"), rules_path %>

<%= render DS::Dialog.new do |dialog| %>
  <%
    title = if @rule.name.present?
              t("rules.form.edit_title_with_name", resource: @rule.resource_type, name: @rule.name)
            else
              t("rules.form.edit_title", resource: @rule.resource_type)
            end
  %>
  <% dialog.with_header(title: title) %>

  <% dialog.with_body do %>
    <%= render "rules/form", rule: @rule %>
  <% end %>
<% end %>
